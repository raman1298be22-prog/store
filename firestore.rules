rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Full open access for development — tighten before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
